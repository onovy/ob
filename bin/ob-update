#!/bin/bash

CHROOTS=(unstable-amd64 stretch-amd64 buster-amd64)

for CHROOT in "${CHROOTS[@]}" ; do
    echo "---------------------------------"
    echo "$CHROOT"
    echo "---------------------------------"
    echo ""
    sudo sbuild-update -udcar "$CHROOT"
done
