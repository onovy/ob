#!/bin/bash

CHROOTS=(unstable-amd64 unstable-i386 jessie-amd64 wheezy-amd64)

for CHROOT in "${CHROOTS[@]}" ; do
    echo "---------------------------------"
    echo "$CHROOT"
    echo "---------------------------------"
    echo ""
    sudo sbuild-update -udcar "$CHROOT"
done
